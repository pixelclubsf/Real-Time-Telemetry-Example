[build-system]
requires = ["setuptools>=45", "wheel", "setuptools_scm[toml]>=6.2"]
build-backend = "setuptools.build_meta"

[project]
name = "solar-regatta"
version = "0.1.0"
description = "A Python package for analyzing and visualizing solar boat race telemetry data"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
    {name = "Charlie Cullen", email = "charliewcullen@gmail.com"},
]
keywords = ["solar", "boat", "telemetry", "analysis", "visualization", "VESC"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering",
    "Topic :: Software Development :: Libraries",
]

dependencies = [
    "plotly>=5.17.0",
    "matplotlib>=3.8.0",
    "numpy>=1.24.3",
    "mgrs>=1.4.6",
    "pyproj>=3.6.0",
]

[project.urls]
Homepage = "https://github.com/charlieijk/SolarRegatta2026"
"Bug Reports" = "https://github.com/charlieijk/SolarRegatta2026/issues"
Source = "https://github.com/charlieijk/SolarRegatta2026"

[project.optional-dependencies]
vesc = [
    "pyserial>=3.5",
]
ml-advanced = [
    "scikit-learn>=1.3.0",
    "xgboost>=2.0.0",
    "lightgbm>=4.0.0",
]
all = [
    "pyserial>=3.5",
    "scikit-learn>=1.3.0",
    "xgboost>=2.0.0",
    "lightgbm>=4.0.0",
]

[project.scripts]
solar-regatta = "solar_regatta.cli:main"

[tool.setuptools]
packages = ["solar_regatta", "solar_regatta.core", "solar_regatta.ml", "solar_regatta.viz", "solar_regatta.vesc"]
include-package-data = true

[tool.setuptools.package-data]
solar_regatta = []
